// Tailwind v4 uses CSS-first config (@theme in globals.css)
// This file is kept for editor tooling only
import type { Config } from "tailwindcss";
const config: Config = { content: ["./src/**/*.{ts,tsx}"] };
export default config;
